import React from "react";

const Form = (props) => {
  return (
    <>
      <h1>Welcome</h1>
      <p>{props.title}</p>
      <input></input>
      <button></button>
    </>
  );
};

export default Form;
